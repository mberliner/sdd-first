{
  "sdd-gate": {
    "PreToolUse": [
      {
        "matcher": "replace_file_content|multi_replace_file_content|write_to_file",
        "hooks": [
          {
            "type": "command",
            "command": "python3 agy_gate_hook.py || python agy_gate_hook.py || type agy_deny.json || cat agy_deny.json"
          }
        ]
      }
    ]
  }
}
